# Competitor Intelligence Dashboard Configuration

# Dashboard settings
dashboard:
  host: '0.0.0.0'
  port: 5000
  debug: true

# Scraping settings
scraping:
  timeout: 30
  user_agent: 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36'

# Note: Competitors are now managed through the web interface
# Add competitors via the dashboard at http://localhost:5000

